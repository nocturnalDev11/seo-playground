specVersion: "v1"
serviceId: "seo-playground"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "seo-playground-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
